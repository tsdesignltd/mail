# MailDeck 📮

mac の「メール」(Mail.app) と連携して動く、メール整理アプリです。

**🧪 デモ (サンプルデータ)**: https://tsdesignltd.github.io/mail/
GitHub Pages は静的ホスティングのため、デモは架空のサンプルデータで動きます。
実際のメール整理は下記の手順でローカル実行してください。

- **迷惑メールの自動判定** — ルールベースで判定理由つきでリストアップし、ワンクリックで専用メールボックス「MailDeck迷惑候補」へ仕訳
- **メッセンジャー風UI** — FBメッセンジャーのように、差出人ごとにメールを会話形式で閲覧
- **よく使う相手の自動学習** — 受信頻度・返信履歴・直近性から重要な差出人を判定し、自動でリスト上部に追加
- **学習するブロック/信頼リスト** — 仕訳・「問題なし」操作が次回以降の判定に反映される

追加インストール不要。macOS 標準の Python 3 だけで動きます。

## 起動方法

```sh
cd "/Users/main/TSDESIGN Dropbox/AI_WORK/claude/maildeck"
python3 server.py
```

ブラウザで **http://localhost:8765** を開き、右上の「同期」を押してください。
Mail.app が起動している必要があります。

初回はターミナルに「"ターミナル"が"メール"を操作しようとしています」という
許可ダイアログが出るので「OK」を押してください。

## 動作モード

| モード | 速度 | 条件 |
|---|---|---|
| **高速モード** (ローカルDB直読み) | 数秒で数千件 | フルディスクアクセスの許可が必要 |
| **AppleScriptモード** (フォールバック) | 1アカウントあたり数十秒〜 | そのまま動く |

### 高速モードを有効にする(推奨)

メール15万通以上ある環境では高速モードを強く推奨します。

1. 「システム設定」→「プライバシーとセキュリティ」→「フルディスクアクセス」
2. MailDeck を起動するアプリ(**ターミナル**、または Claude)をオンにする
3. ターミナルを再起動して `python3 server.py` を再実行

※ この設定はメールDBの読み取りにのみ使われます。仕訳(移動)は常に AppleScript
   経由で Mail.app が行うため、DBに書き込むことはありません。

## 迷惑メールの仕訳先

「MailDeck迷惑候補」という**ローカルメールボックス(このMac内)**に移動します。
Mail.app のサイドバーの「このMac内」に表示されます。誤判定してもメールは
削除されず、手で受信トレイに戻せます。

## AI判定(オプション)

Claude Code CLI (`claude` コマンド) がインストールされている場合、
グレーゾーンのメールを「🤖 グレーゾーンをAI判定」ボタンでまとめて判定できます。
CLI が無い場合はボタンが表示されません(手動で「迷惑」/「問題なし」を選択)。

## データの保存場所

- `data/settings.json` — 信頼/ブロックリスト、よく使う相手、設定
- `data/cache.json` — 取得済みメールのメタデータキャッシュ

メール本文はキャッシュせず、表示時に Mail.app から都度取得します。
すべてローカルに保存され、外部には送信されません(AI判定を使った場合のみ
件名・差出人が Claude API に送られます)。

## 仕組み

```
ブラウザUI (public/)
   ↕ HTTP (localhost:8765)
server.py — API・迷惑判定 (rules.py)・重要度スコアリング
   ↕
mail_store.py — 高速モード: ~/Library/Mail の Envelope Index (SQLite, 読み取り専用)
             — フォールバック: AppleScript (scripts/*.applescript)
   ↕
Mail.app(仕訳・本文取得は常に AppleScript 経由)
```
